function titleToNumber(columnTitle: string): number {
  let result = 0;
  while (columnTitle.length > 0) {
    const char = columnTitle[columnTitle.length - 1];
    columnTitle = columnTitle.slice(0, columnTitle.length - 1);
  }
  return 0;
}
